Добавил только графические формы без привязки слотов.

Методы добавления и удаления контактов не работают корректно.
На примере добавления (client.py стр. 217 add_contact()):
- добавляются данные в базы,
- формируется запрос на стороне клиента,
- отправляется запрос серверу (стр. 226),
- сервер отправляет подтверждение (server.py стр. 157),

- ... и клиент зависает на стр. 227 (get_message), ждет ответа от сервера.
Ответ сервера на подтверждение добавления контакта где-то пропадает.
Вывести клиент "из сна" можно только отправив ему какое-нибудь сообщение.
Но тогда это сообщение он обрабатывает как раз в стр. 227 (get_message), где завис.
Поэтому воспринимает сообщение как некорректное.

С удалением контакта происходит то же самое.

Перестало работать то, что раньше работало.
Команда exit клиента стала вызывать падение сервера из-за того, что происходит
обращение к БД из другого потока (раньше такой ошибки не возникало).
Отключил в настройках базы проверку потоков. Но, естественно, сервер перестал
объяснять причину, но так же отключается.

Отправляю как есть. Попробую потом с нуля, с того места, где работало, все заново переписать.