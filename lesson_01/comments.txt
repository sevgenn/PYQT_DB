1. Первые три задания на написание функций изначально делал из-под Linux, все работало корректно.
   Перед отправкой протестировал под виндой, понял, что в Windows в одноранговых сетях
   возвращается код 0 (returncode), если даже хост не пингуется.
   Первые два задания переделывать не стал, оставил рабочий вариант для Linux, а в третьем заменил
   проверку доступности через проверку наличия "TTL" в строке вывода. Умнее ничего не придумал, но
   работает на обеих платформах. Хотел попробовать pyping, но его, видимо, надо устанавливать
   глобально, в виртуальном окружении не зашел, поэтому не стал тестировать.

2. В "Месенджере" попробовал переписать Сервер и Клиент через классы. Launcher на запуск Сервера и
   двух Клиентов. Я брал перерыв в учебе. После перерыва в свой старый код трудно вникнуть, поэтому
   получилось, наверное, не очень.

3. За дополнительное задание не брался, хотел начать с понедельника. Но у меня сразу ВОПРОС по базам:

	Не получается работать с postgres только средствами python. Т.е. если я заранее в postgres
	создаю базу, то я могу к ней обратиться с помощью python по имени базы и дальше работать.
	Но если база заранее не создана, и я пытаюсь подключиться к postgres по типу:
	psycopg2.connect(host=hostname, user=username, password=password), чтобы потом создать
	базу внутри python, у меня постоянно ошибка о невозможности подключения.
	Возможно создать базу postgres исключительно средствами python?
	Если да, можно это на уроке коротко показать?